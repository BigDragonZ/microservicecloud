spring:
  application:
    name: microservicecloud-config-client  # 和bootstrap配置里面name一致
  profiles:
    active:
      - dev
---


spring:
  profiles: dev
  application:
    name: microservicecloud-config-atguigu-dev
---


spring:
  profiles: test
  application:
    name: microservicecloud-config-atguigu-dev

